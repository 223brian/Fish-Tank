[Unit]
Description=Fish Tank Backend API
After=network.target

[Service]
User={USERNAME}
WorkingDirectory={PATH_TO_BACKEND_DIR}
ExecStart={PATH_TO_BACKEND_DIR}/venv/bin/uvicorn main:app --host 0.0.0.0 --port 8000
Restart=always

[Install]
WantedBy=multi-user.target
